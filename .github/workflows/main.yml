name: setup node
run-name: ${{ github.actor }} Started a process
on:
  push:
    branches:
    - main
jobs:
  setup:
    name: setup node
    runs-on: ubuntu-latest
    steps:
      - run: apt-get install node
      - run: node -v
